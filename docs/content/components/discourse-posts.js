import React from "react";
import PropTypes from "prop-types";

const DiscoursePosts = ({ apiUrl }) => {
  return <div>Latest Posts!</div>;
};

DiscoursePosts.propTypes = {
  apiUrl: PropTypes.string,
};

export default DiscoursePosts;
