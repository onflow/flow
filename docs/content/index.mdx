---
title: Getting Started
description: "Welcome to the Flow Developer Portal"
sidebar: false
---

import DocsetMenu from "./components/docset-menu";
import { MenuTitle } from "./components/menu";
import { theme } from "gatsby-theme-flow/src/colors";

Before starting your journey on Flow we recommend reading the <a href="https://www.onflow.org/primer">Flow Primer</a>. It's a quick introduction to how the Flow blockchain works. It will be easier to understand the things you find here if you have a basic idea of how Flow works.

## Install Flow

To work with Flow locally you'll need to install the <a href="https://github.com/onflow/flow/blob/master/docs/cli.md#flow-cli">Flow CLI</a>.
The Flow CLI is a command-line interface that provides useful utilities for building Flow applications, including the [Flow Emulator](/emulator).

<MenuTitle>OSX & Linux</MenuTitle>

```shell
sh -ci "$(curl -fsSL https://storage.googleapis.com/flow-cli/install.sh)"
```

<MenuTitle>Windows</MenuTitle>

```powershell
iex "& { $(irm 'https://storage.googleapis.com/flow-cli/install.ps1') }"
```

---

## Start Building

Ready to start building? Take a tutorial, or have a look at an example application.

### 🏃‍♂️ App Quickstart using React

Follow the [Flow App Quickstart](/flow-js-sdk/flow-app-quickstart/) for a detailed guide covering how to use the [Flow JS SDK](/flow-js-sdk) to build a serverless app that connects to Flow, using React!

### **😸 Kitty Items** Full-Stack Example App

The Kitty Items application is a full-featured NFT marketplace that demonstrates what a complex full-stack appliction
built on Flow will look like. It's designed to showcase the power of Cadence and Flow's client libraries.
Have a look at the [Kitty Items Github repository](https://github.com/onflow/kitty-items) for more details.

---

## Explore

Flow was designed for builders. It's a "batteries included" platform complete with developer tools you can use to start building right away.

<DocsetMenu />
