version: v1
deps:
  - buf.build/googleapis/googleapis
build:
  excludes:
    # exclude Gradle build dir
    - build/
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  enum_zero_value_suffix: UNKNOWN
  service_suffix: API
